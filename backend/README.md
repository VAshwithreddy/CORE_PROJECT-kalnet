# Backend

This folder is reserved for backend code.

Add backend API, database, authentication, server configuration, and service files here when the backend stack is selected.

Recommended future structure:

```text
backend/
  src/
  tests/
  docs/
  .env.example
  README.md
```

Do not commit real passwords, API keys, or production database URLs.
