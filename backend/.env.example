CORE_ENV=development
CORE_PROJECT_NAME=CORE API
CORE_ALLOWED_ORIGINS=http://localhost:3000
# Set a unique value before any production deployment.
SECRET_KEY=
# Keep this enabled only for local seeded-data development.
CORE_ALLOW_DEV_PASSWORDLESS_LOGIN=true
# Required in production. Store the Firebase service-account JSON as a secret.
FIREBASE_SERVICE_ACCOUNT_JSON=
